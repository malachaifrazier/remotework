%h1 Post a Job

= render partial: 'why_post'
= render partial: 'shared/flash'
    
= form_for @job, url: jobs_path do |f|
  = render partial: 'form', locals: { f: f }
  .text-center
    = f.submit "Preview your post", class: 'btn btn-lg btn-primary preview'
